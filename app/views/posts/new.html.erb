<div class="main post-wrapper">
    <%= form_for @post do |f| %>
        <div class="field">
            <%= f.text_area :content, size: "30x10" %>
            <%= f.select :tag, Post.tags.keys.to_a, {} %>
            <%= f.submit "ツイートする",  class: "btn btn-primary btn-lg"%>
        </div>
    <% end %>
</div>