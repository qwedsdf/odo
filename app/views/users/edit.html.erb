<div class="edit-wrapper">
    <%= form_with url: users_update_url ,method: :post do |f| %>
        <label>名前</label>
        <%= f.text_field :name %>
        <label>アイコン</label>
        <%= f.file_field :icon %>
        <%= f.submit "更新する",  class: "btn btn-primary btn-lg"%>
    <% end %>
</div>